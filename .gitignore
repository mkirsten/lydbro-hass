# Python byte-code (picked up on any dev machine that runs HA locally)
__pycache__/
*.py[cod]

# Editor / OS junk
.DS_Store
.idea/
.vscode/

# Release artifacts (the release workflow builds lydbro.zip on the runner)
*.zip

# Local secrets — never commit tokens
.env

# Dev venv and pytest caches
.venv/
.pytest_cache/
.coverage
htmlcov/
