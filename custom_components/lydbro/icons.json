{
  "entity": {
    "binary_sensor": {
      "ble_connected": { "default": "mdi:bluetooth-connect" },
      "ethernet": { "default": "mdi:ethernet" },
      "safe_mode": { "default": "mdi:shield-alert" }
    },
    "sensor": {
      "battery": { "default": "mdi:battery" },
      "boot_phase": { "default": "mdi:progress-clock" },
      "firmware_version": { "default": "mdi:chip" },
      "ip_address": { "default": "mdi:ip-network" }
    },
    "button": {
      "reboot": { "default": "mdi:restart" },
      "reset_pairing": { "default": "mdi:bluetooth-off" },
      "ble_disconnect": { "default": "mdi:bluetooth-off" }
    },
    "event": {
      "button": { "default": "mdi:gesture-tap-button" },
      "menu": { "default": "mdi:menu" },
      "scene": { "default": "mdi:palette-swatch" }
    },
    "remote": {
      "remote": { "default": "mdi:remote" }
    }
  },
  "services": {
    "send_remote_key": { "service": "mdi:remote" }
  }
}
